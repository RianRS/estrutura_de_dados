'Questão 7'
# Veja o código que usa a definição de operador de fatia
lyst1 = [2, 4, 8]
lyst2 = list(lyst1)
print(lyst1 is lyst2)
print(lyst1 == lyst2)
# A operação == para os dois conjuntos deve:
# (a) Comparar pares de itens em cada posição quanto à igualdade.
# (b) Verificar se os conjuntos são do mesmo tamanho e se cada item
# em um conjunto também está no outro conjunto.
# (c) Retorna saída False para todos os itens posição à posição.
# (d) Compara o tamanho dos dois conjuntos em questão.
# RESPOSTA: item(a)


'Questão 8'
# Qual a hierarquia correta de complexidade computacional?
#
# (a) O(n** 3) > O(2 ** n) > O(ln(n)) > O(1).
# (b) O( n** 3) > O(ln(n)) > O(nln(n)) > O(1).
# (c) O(2n) > O(n ** 3) > O(nln(n)) > O(ln(n)).
# (d) O(n ** 3) > O(n ** 2) > O(ln(1)) > O(1).
# RESPOSTA: item(c)